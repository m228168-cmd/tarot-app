- 修正 heartbeat / silence 規則混淆：`HEARTBEAT_OK` 只能用於真正的 heartbeat poll，不得作為一般 Discord/人類訊息的靜默回覆。
- 一般聊天若應保持安靜，應回 `NO_REPLY`；已同步更新 `AGENTS.md` 與 `HEARTBEAT.md`，避免再次把 heartbeat ack 直接露到聊天介面。
- `ai-auto-editor` 已重新驗證並打通 Google Drive 下載流程；成功下載至少兩份素材到 `downloads/raw/`。
- 已修正下載任務狀態不同步問題，新增 `pipeline:download-latest:task` 與 `recover-task-from-log.js`。
- 已在本機安裝 `openai-whisper` + `torch`，並修正 `transcribe.js` 可直接呼叫本地 whisper CLI。
- 已成功對素材 `huv-zmzp-nog (2026-04-01 21:53 GMT+8)` 產生 transcript JSON、SRT 字幕，以及初版 `cut-suggestions.json`；目前 pipeline 已具備下載→轉錄→產出字幕/剪輯建議的最小可用鏈路。
- 使用者新增偏好：當他說「報告剩餘用量」時，預設同時回報 Codex 與 Claude 兩邊的剩餘用量；若工具受限拿不到其中一邊，要明講哪邊拿不到。
- `ai-auto-editor` 已完成完整 rough cut 版本：自動剪除較長空白、保留完整節目內容、輸出 soft-sub MP4（內嵌可切換字幕），並上傳回原 Google Drive `output` 資料夾。正式成品 fileId=`1lN-eNrsjDCCf--DEUjZ7QTl9HSTjC_TT`；測試上傳檔已刪除避免污染 output。
